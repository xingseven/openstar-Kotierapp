# 版本检查接口文档

## 接口地址

App 通过更新 API 获取版本信息：

```
https://kotier.wotty.app/version.json
```

## 兼容地址

当前版本使用：

```
https://kotier.wotty.app/version.json
```

已发布的 4.2.45 客户端使用：

```
https://kotier.openstars.org/version.json
```

两个地址都必须持续返回兼容格式，旧地址不能下线。

## JSON 格式

```json
{
  "latest_version": "4.2.46",
  "latest_version_code": 4065,
  "download_url": "https://kotier.wotty.app/download/kotier-v4.2.46-4065-release.apk",
  "source_download_url": "https://github.com/sevencnup/wotty-kotierapp/releases/download/v4.2.46/kotier-v4.2.46-4064-release.apk",
  "release_notes": "4.2.46 发版"
}
```

## 字段说明

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| latest_version | string | 是 | 最新版本号，如 "4.2.45"（不要带 v 前缀） |
| latest_version_code | number | 否 | APK 的 Android versionCode；版本名相同时用于识别修复包或重签名包 |
| download_url | string | 是 | APK 下载地址 |
| source_download_url | string | 否 | 对应的 GitHub Release 源地址，供排查和管理员核对 |
| release_notes | string | 否 | 更新说明 |

## 数据来源与兼容策略

更新服务按请求读取 GitHub Releases，并缓存 60 秒内的版本信息；APK 会先下载到服务器持久化缓存，再由自有域名提供下载：

1. App 请求 `/version.json`。
2. 服务读取 `sevencnup/wotty-kotierapp` 的最新 Release。
3. 服务返回旧客户端兼容的 `latest_version`、`download_url`、`release_notes` 字段，并附带 `source_download_url`。
4. 服务首次发现新 APK 时将其下载到服务器，完成后才对外提供正式文件；下载中使用 `.part` 临时文件。
5. APK 下载链接指向服务器对应 Release 的版本化缓存文件，不覆盖历史安装包。

因此发布新的 GitHub Release 后，两个域名的更新清单会自动切换到新版本，不需要手动替换 JSON 文件。

## 更新版本流程（管理员）

1. 在 GitHub 上推送 `v*` 标签，Actions 自动编译并创建 Release。
2. 确认 Release 中存在 APK 资产。
3. 更新服务会自动返回最新版本。

## 注意事项

- 确保服务器支持 HTTPS（现代 Android 要求）
- APK 使用版本化文件名保存，下载地址指向服务器缓存，不覆盖旧版本包；服务器仍保留 GitHub Release 源地址用于自动同步
- 版本号格式建议使用语义化版本：`主版本.次版本.修订号`
- `release_notes` 直接取 GitHub Release 的正文，App 发现新版本时会在弹窗中显示正文，并提供“下载更新”按钮。
- 更新弹窗同时提供“浏览器下载安装包”入口，打开服务器 APK 直链；如果遇到签名不一致，请手动下载安装包并更新。
- 服务从 APK 文件名中的 `-versionCode-release.apk` 读取 `latest_version_code`，同一个版本名发布修复包时选择版本代码更高的 APK。
