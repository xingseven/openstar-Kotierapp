# 版本检查接口文档

## 接口地址

App 通过更新 API 获取版本信息：

```
https://kotier.wotty.app/version.json
```

## 兼容地址

当前版本使用：

```
https://kotier.wotty.app/version.json
```

已发布的 4.2.45 客户端使用：

```
https://kotier.openstars.org/version.json
```

两个地址都必须持续返回兼容格式，旧地址不能下线。

## JSON 格式

```json
{
  "latest_version": "4.2.46",
  "download_url": "https://github.com/sevencnup/wotty-kotierapp/releases/download/v4.2.46/kotier-v4.2.46-4064-release.apk",
  "release_notes": "4.2.46 发版"
}
```

## 字段说明

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| latest_version | string | 是 | 最新版本号，如 "4.2.45"（不要带 v 前缀） |
| download_url | string | 是 | APK 下载地址 |
| release_notes | string | 否 | 更新说明 |

## 数据来源与兼容策略

更新服务按请求读取 GitHub Releases，并缓存 60 秒内的结果：

1. App 请求 `/version.json`。
2. 服务读取 `sevencnup/wotty-kotierapp` 的最新 Release。
3. 服务返回旧客户端兼容的 `latest_version`、`download_url`、`release_notes` 字段。
4. APK 下载链接指向对应 Release 的版本化资产，不覆盖历史安装包。

因此发布新的 GitHub Release 后，两个域名的更新清单会自动切换到新版本，不需要手动替换 JSON 文件。

## 更新版本流程（管理员）

1. 在 GitHub 上推送 `v*` 标签，Actions 自动编译并创建 Release。
2. 确认 Release 中存在 APK 资产。
3. 更新服务会自动返回最新版本。

## 注意事项

- 确保服务器支持 HTTPS（现代 Android 要求）
- APK 使用版本化文件名保存，下载地址指向 GitHub Release，不覆盖旧版本包
- 版本号格式建议使用语义化版本：`主版本.次版本.修订号`
- `release_notes` 直接取 GitHub Release 的正文，App 发现新版本时会在弹窗中显示正文，并提供“下载更新”按钮。
