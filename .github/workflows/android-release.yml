name: Android Release

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    name: Build APK
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          cache-read-only: false

      - name: Create local.properties
        working-directory: ./android
        run: echo "sdk.dir=$ANDROID_HOME" > local.properties

      - name: Decode release keystore
        env:
          KOTIER_RELEASE_KEYSTORE_BASE64: ${{ secrets.KOTIER_RELEASE_KEYSTORE_BASE64 }}
        run: |
          test -n "$KOTIER_RELEASE_KEYSTORE_BASE64"
          echo "$KOTIER_RELEASE_KEYSTORE_BASE64" | base64 --decode > android/kotier-release.jks

      - name: Build Release APK
        working-directory: ./android
        env:
          KOTIER_RELEASE_STORE_FILE: kotier-release.jks
          KOTIER_RELEASE_STORE_PASSWORD: ${{ secrets.KOTIER_RELEASE_STORE_PASSWORD }}
          KOTIER_RELEASE_KEY_ALIAS: ${{ secrets.KOTIER_RELEASE_KEY_ALIAS }}
          KOTIER_RELEASE_KEY_PASSWORD: ${{ secrets.KOTIER_RELEASE_KEY_PASSWORD }}
        run: |
          chmod +x gradlew
          ./gradlew assembleRelease --no-daemon

      - name: Upload APK Artifact
        if: ${{ !startsWith(github.ref, 'refs/tags/v') }}
        uses: actions/upload-artifact@v4
        with:
          name: kotier-release-apk
          path: android/app/build/outputs/apk/release/*-release.apk

      - name: Create Release
        if: startsWith(github.ref, 'refs/tags/v')
        uses: softprops/action-gh-release@v2
        with:
          files: android/app/build/outputs/apk/release/*-release.apk
          generate_release_notes: false
          body_path: RELEASE_NOTES.md
