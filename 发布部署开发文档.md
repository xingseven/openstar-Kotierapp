# Kotier 4.2.46 发布与更新服务部署开发文档

## 目标

恢复已发布 4.2.45 客户端依赖的 `kotier.openstars.org` 更新入口，同时部署新的 `kotier.wotty.app` 更新入口，并将 Kotier 客户端升级到 4.2.46 后完成 GitHub Release 发版。

## 执行步骤

1. [x] 检查本地工作区、当前版本、Git 远端、发版工作流和现有更新实现。
2. [x] 通过 Ops Control 发现服务器上的现有项目和服务，确认旧域名当前被 WottyGIF 页面占用。
3. [x] 恢复 `https://kotier.openstars.org/version.json` 的服务器路由，保持 4.2.45 客户端兼容；历史 APK 不删除、不覆盖。
4. [x] 在同一服务器部署动态更新服务；两个域名已配置到动态 API 的上游端口，旧域名和新域名 HTTPS 均已可访问。
5. [x] 将客户端更新检查地址改为 `https://kotier.wotty.app/version.json`，升至 4.2.46，并补充版本记录与接口文档。
6. [x] 按项目现有方式执行本地 Release 构建，确认 APK 产物、版本号、版本代码和更新地址正确。
7. [x] 将本次本地修改提交到 Kotier 仓库。
8. [x] 按用户明确要求推送 `main`、创建 4.2.46 发版标签并验证 GitHub Actions / Release 产物。
9. [x] 线上已验证旧域名和新域名 JSON 均可访问并返回 4.2.46；4.2.45 兼容入口已保留。
10. [x] 将 GitHub Release APK 自动缓存到更新服务器，并通过两个自有域名提供带 Range 的下载。
11. [x] 清理本次产生的一次性脚本和临时进程，更新文档勾选状态。

## 约束

- 不删除 `kotier.openstars.org`，不破坏已经发布的 4.2.45。
- 不覆盖旧版本 APK，所有安装包使用版本化文件名。
- Ops Control 只使用受控的服务器发现、项目部署和反向代理能力，不读取或输出密码、环境变量、私钥。
- 不执行危险的 Docker 清理操作。
- GitHub 推送和 Release 创建只在本开发文档前置本地验证、提交完成后进行。
- 若服务器现状无法安全复用或需要用户提供无法从本地/服务器发现的信息，停止在对应步骤并报告具体阻塞点。
- 更新说明继续直接取 GitHub Release 正文；安装包下载地址改为服务器缓存地址，减少国内用户访问 GitHub 的速度和稳定性问题。
- 更新弹窗必须提供浏览器手动下载入口；下载接口返回 APK 附件响应，便于签名冲突时手动下载安装。
- Android Release 构建禁止使用 Debug keystore；必须使用固定且不提交仓库的正式 keystore。本地构建与 GitHub Actions 必须使用同一把签名。
- 如果找不回 4.2.45 的原始签名私钥，新的正式签名只能保证后续版本连续升级，不能让 4.2.45 无损覆盖升级。

## 本次后续处理

1. 更新弹窗显示提示：`如果遇到签名不一致，请手动下载安装包并更新。`
2. 点击手动下载时调用系统浏览器打开服务器 APK 直链；服务器以附件方式响应，直接触发下载。
3. 将 Release 从 Debug 签名切换到固定正式签名；签名文件、密码和私钥不进入 Git。
4. 发布前必须用 `apksigner` 核对本地 APK 与 GitHub APK 的证书指纹一致。
5. GitHub Actions 发版前必须配置 `KOTIER_RELEASE_KEYSTORE_BASE64`、`KOTIER_RELEASE_STORE_PASSWORD`、`KOTIER_RELEASE_KEY_ALIAS` 和 `KOTIER_RELEASE_KEY_PASSWORD` Secrets；未配置时构建应直接失败，不得回退到 Debug 签名。
6. 同版本名修复包使用更高的 Android versionCode 和版本化文件名，更新服务按 versionCode 选择同一 Release 中较新的 APK，不覆盖历史安装包。

## 4.2.46 更新说明与下载链接修复

1. [x] 恢复 GitHub Release 的完整更新说明，禁止自动生成的 `Full Changelog` 覆盖正文。
2. [x] 在更新说明中加入服务器 APK 下载链接。
3. [x] App 更新弹窗将普通 URL 渲染为可点击链接，并保留明显的浏览器下载按钮。
4. [x] 使用 versionCode 4066 构建新的正式签名修复包，避免复用 4065 缓存。
5. [x] 本地验证后提交，并更新 GitHub Release 与服务器自动同步结果。
